services:
  guarrdian:
    image: ghcr.io/helico-pter/guarrdian:latest
    build: .
    container_name: guarrdian
    restart: unless-stopped
    ports:
      - "9988:9988"
    volumes:
      # All persistent data (config.yml and reviews.db) lives here
      - ./config:/app/config
